[tool.ruff]
line-length = 88
target-version = "py311"
select = ["E", "F", "B", "I", "UP", "PL"]

[tool.ruff.lint]
select = ["UP"]

[tool.vulture]
min_confidence = 80
paths = ["app.py"]
exclude = [".venv", "venv"]
