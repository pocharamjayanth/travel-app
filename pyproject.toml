[tool.ruff]
line-length = 88
target-version = "py311"
# UP enables pyupgrade checks natively within Ruff
select = ["E", "F", "B", "I", "UP", "PL"]

[tool.vulture]
min_confidence = 80
paths = ["app.py"]
exclude = [".venv", "venv"]

[tool.pylint]
max-line-length = 88
