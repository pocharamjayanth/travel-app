# AI Agent System Architecture
This repository implements a decentralized, hybrid AI architecture built to optimize user privacy.
### 1. Dynamic UI Translation Layer
Manages the application's layout internationalization engine (`i18n`).
### 2. Hybrid Execution Gateway Middleware
Intercepts itinerary prompt strings and routes to Ollama (local) or OpenAI (cloud).
